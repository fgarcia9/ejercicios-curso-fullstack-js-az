/**expression functions. expression functions cannot be called before declaration*/

const calcSumExpr= function(number1,number2) {
    const res = number1 + number2;
    return res;
}

console.log(calcSumExpr(8,4));











