/**JSON data format. converting a object to a json and viceversa */

const user = {
    name : "Francisco",
    age : 31,
};

const jsonData = JSON.stringify(user);

console.log(JSON.parse(jsonData));

console.log(jsonData);










